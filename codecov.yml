coverage:
  status:
    project:
      default:
        target: auto
        threshold: 2%
    patch:
      default:
        target: 90%

ignore:
  - 'shared/**'
  - '**/index.ts'
  - '**/*.d.ts'
  - 'scripts/**'
