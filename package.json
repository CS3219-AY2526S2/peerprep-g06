{
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "build": "npm run build --prefix frontend && npm run build --prefix services/user-service && npm run build --prefix services/question-service && npm run build --prefix services/matching-service",
    "typecheck": "npm run typecheck --prefix frontend && npm run typecheck --prefix services/user-service && npm run typecheck --prefix services/question-service && npm run typecheck --prefix services/matching-service",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.1.18",
    "prettier": "^3.8.1"
  },
  "dependencies": {
    "@radix-ui/react-label": "^2.1.8",
    "@radix-ui/react-slot": "^1.2.4",
    "class-variance-authority": "^0.7.1",
    "clsx": "^2.1.1",
    "lucide-react": "^0.575.0",
    "tailwind-merge": "^3.5.0"
  }
}
