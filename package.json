{
  "engines": {
    "node": "24.x"
  },
  "scripts": {
    "build": "npm run build --prefix frontend && npm run build --prefix services/user-service && npm run build --prefix services/question-service && npm run build --prefix services/matching-service && npm run build --prefix services/collaboration-service && npm run build --prefix shared/rabbitmq",
    "typecheck": "npm run typecheck --prefix frontend && npm run typecheck --prefix services/user-service && npm run typecheck --prefix services/question-service && npm run typecheck --prefix services/matching-service && npm run typecheck --prefix services/collaboration-service && npm run typecheck --prefix shared/rabbitmq",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4.1.18",
    "@types/amqplib": "^0.10.8",
    "@types/express": "^5.0.6",
    "@types/node": "^25.5.0",
    "nodemon": "^3.1.14",
    "prettier": "^3.8.1",
    "ts-node": "^10.9.2",
    "tsconfig-paths": "^4.2.0",
    "typescript": "^6.0.2"
  },
  "dependencies": {
    "@radix-ui/react-label": "^2.1.8",
    "@radix-ui/react-slot": "^1.2.4",
    "amqplib": "^0.10.9",
    "class-variance-authority": "^0.7.1",
    "clsx": "^2.1.1",
    "express": "^5.2.1",
    "lucide-react": "^0.575.0",
    "module-alias": "^2.3.4",
    "tailwind-merge": "^3.5.0"
  }
}
